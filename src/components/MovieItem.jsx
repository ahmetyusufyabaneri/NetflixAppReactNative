import {Image, TouchableOpacity, View} from "react-native";

const MovieItem = ({movie}) => {
  console.log(movie);
  return (
    <TouchableOpacity>
      <View></View>
    </TouchableOpacity>
  );
};

export default MovieItem;
